
# Create the complete updated package.json
package_json = '''{
  "name": "sitesawa",
  "version": "2.0.0",
  "description": "Website template store with M-Pesa payments and Render hosting. Customers bring their own domain.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "axios": "^1.7.0",
    "nodemailer": "^6.9.0",
    "helmet": "^7.2.0",
    "express-rate-limit": "^7.5.0",
    "validator": "^13.12.0",
    "dotenv": "^16.4.0"
  }
}
'''

with open('/mnt/agents/output/package.json', 'w') as f:
    f.write(package_json)

print("package.json created")
